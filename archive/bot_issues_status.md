# Статус по `bot_issues.md` перед финальными тестами

Дата фиксации: 2026-04-21

Этот файл — рабочая сводка по фактическому состоянию пунктов из `bot_issues.md`.
Оригинальный `bot_issues.md` не переписывается и остается источником исходных задач.

---

## 🔴 #1 — CTA слишком поздно
**Статус:** ✅ Сделано (в улучшенном варианте)

Что сделано:
- Добавлена настройка `cta_from_turn` на уровне YAML документа.
- Дефолт: `0` (CTA можно с первого содержательного ответа по теме).
- В `policy.py` порог CTA теперь берется из метаданных документа, а не зашит глобально.

Файлы:
- `meta_loader.py`
- `policy.py`

---

## 🔴 #2 — История диалога пишется дважды
**Статус:** ✅ Сделано

Что сделано:
- Убраны дубли `mem_add_user` / `mem_add_bot` из `llm.py`.
- История пишется централизованно в `_service_reply` (`app.py`).

Файлы:
- `llm.py`

---

## 🔴 #3 — pymorphy3 может не быть в зависимостях
**Статус:** ⚠️ Частично

Что есть:
- В `requirements.txt` есть `pymorphy3`.

Что не закрыто:
- Нет `pymorphy3-dicts-ru`.

Файлы:
- `requirements.txt`

---

## 🔴 #4 — Лиды сохраняются только в файлы на сервере
**Статус:** ✅ Сделано (demo-safe вариант)

Что сделано:
- Основной путь доставки лида: e-mail на адрес клиента.
- Если e-mail не отправился, включается fallback: сохранение JSON в `leads/`.
- Зафиксирован минимальный контракт результата `handle_lead()`:
  - `ok: bool`
  - `error_code: str | null`
  - `delivery: "email" | "file_fallback" | null`
- Контракт используется и в `/lead`, и в чат-flow (через `lead_error` мета-флаг).

Комментарий:
- CRM/n8n/webhook не внедрялись осознанно (следующий этап).

Файлы:
- `lead_service.py`
- `app.py`
- `flow_handlers.py`

---

## 🔴 #5 — Flask + SQLite и несколько воркеров
**Статус:** ✅ Сделано

Что сделано:
- Прод-старт в контейнере переведен на `gunicorn` с одним воркером.
- Зафиксирован запуск: `gunicorn -w 1 -b 0.0.0.0:8000 app:app`.
- Добавлен `gunicorn` в зависимости проекта.
- Добавлен явный entrypoint `start.sh` с тем же режимом `-w 1`.

Файлы:
- `Dockerfile`
- `requirements.txt`
- `start.sh`

---

## 🔴 #6 — LLM rerank работает хуже, чем мог бы
**Статус:** ✅ Сделано

Что сделано:
- Исправлена формулировка промпта.
- Усилен системный промпт rerank под домен стоматологии.
- В кандидаты передается `h2 + h3 + text`.
- Rerank переведен на JSON-контракт `{"choice": ...}`.
- Добавлена строгая валидация `choice` + fallback + `fallback_reason` в лог.

Файлы:
- `retriever.py`

---

## 🟡 #7 — Короткие реплики после followup кнопки
**Статус:** ✅ Сделано (UX-улучшенный вариант)

Что сделано:
- При `offered_subtopic` и коротком `да`:
  - если кнопка одна -> переход по `redirect_ref`,
  - если кнопок 2+ -> уточняющий вопрос + повтор кнопок (без автопрыжка в первую).
- Добавлена обработка `redirect_ref` в `app.py`.

Файлы:
- `flow_handlers.py`
- `app.py`

---

## 🟡 #8 — "Ситуация" не передается в LLM
**Статус:** 🧭 Осознанно не делаем (архитектурное решение)

Принятое решение:
- `situation_note` не передается в retrieval/LLM.
- `situation_note` используется как контекст лида и уходит в lead pipeline (CRM/n8n).

Почему:
- Более чистая и предсказуемая архитектура.
- Ниже риск увода ответа от фактического контента.

---

## 🟡 #9 — Мертвые поля в YAML (verbatim, preferred_format)
**Статус:** ✅ Сделано

Что сделано:
- `verbatim` и `preferred_format` подключены в сборке промпта LLM.

Файлы:
- `llm.py`

---

## 🟡 #10 — Мертвый код в chunk_doc_type
**Статус:** ✅ Сделано

Что сделано:
- Удален legacy-блок с `item[0]` в `chunk_doc_type`.
- Оставлена актуальная ветка для `dict`.

Файлы:
- `retriever.py`

---

## 🟡 #11 — YES_RX не покрывает все варианты согласия
**Статус:** ✅ Сделано

Что сделано:
- Расширен словарь согласия.
- Добавлена поддержка коротких форм с префиксом `да` (например, `да, давайте`).

Файлы:
- `session.py`

---

## 🟡 #12 — extract_name не распознает двусловные имена
**Статус:** ✅ Сделано

Что сделано:
- Добавлен разбор формата `Имя Фамилия` в `extract_name()`.

Файлы:
- `session.py`

---

## 🟡 #13 — corpus_alias_leader итерирует весь корпус
**Статус:** ✅ Сделано

Что сделано:
- Добавлен alias-индекс при загрузке корпуса (`alias_term -> список чанков`).
- Поиск лидера теперь сначала сужает кандидатов через индекс.
- Сохранен fallback на полный перебор, если индекс не дал кандидатов.

Файлы:
- `retriever.py`

---

## 🟡 #14 — Нет webhook/callback для проверки доставки лида
**Статус:** ⚠️ Частично (для demo закрыто минимально)

Комментарий:
- Полноценный webhook/callback не внедряли.
- Добавлено fail-soft поведение: при ошибке e-mail лида сохраняется file fallback.
- Добавлено техническое логирование ошибок доставки (`error_code`, события fallback).

Файлы:
- `lead_service.py`

---

## Короткая фиксация финальной архитектуры

- Бизнес-решения остаются в `policy.py`.
- Контентные настройки задаются через YAML/frontmatter (включая `cta_from_turn`).
- Rerank вызывается выборочно и работает по строгому JSON-контракту.
- При неоднозначном `да` на несколько followup-кнопок бот не принимает решение за пользователя.
- `situation_note` — часть lead-контура, не сигнал для LLM/retrieval.
- Debug-роуты в `prod` отключены, в непроизводственной среде требуют токен.
- `/lead` использует ту же проверку `client_id`, что и `/ask`.
- Телефон нормализуется единым backend-правилом в формат `+7XXXXXXXXXX`.
- Логи маскируют PII (телефон, длинные поля `situation*`).
